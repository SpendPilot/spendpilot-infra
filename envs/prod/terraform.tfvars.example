// Non-secret settings now live in variables.tf defaults.
// Keep real secret values out of git and inject them with:
// - TF_VAR_postgres_admin_password, or
// - Azure Key Vault fetched during CI.

postgres_admin_password = "replace-me"

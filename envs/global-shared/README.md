# Global Shared

Planned owner for resources shared by every environment:

- ACR
- primary Azure DNS zone for `costpilot.online`
- legacy Azure DNS zone retention for `myfinagent.online` while historical records or rollback references still matter
- global identities if needed
- other truly global shared services
